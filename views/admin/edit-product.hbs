<section>
    <div class="container mt-4">
        <div class="row justify-content-start mb-3">
            <div class="col-md-2">
                <a href="/admin" class="btn btn-outline-success back-button">
                    <i class="fas fa-arrow-left"></i> Back
                </a>
            </div>
        </div>
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h2 class="text-center animated-title">Edit Product</h2>
                <form action="/admin/edit-product/{{product._id}}" method="post" enctype="multipart/form-data" class="stylish-form">
                    <label for="" class="stylish-label">Name</label>
                    <input type="text" name="Name" class="form-control stylish-input" value="{{product.Name}}">
                    
                    <label for="" class="stylish-label">Category</label>
                    <input type="text" name="Category" class="form-control stylish-input" value="{{product.Category}}">

                    <label for="" class="stylish-label">Price</label>
                    <input type="text" name="Price" class="form-control stylish-input" value="{{product.Price}}">

                    <label for="" class="stylish-label">Description</label>
                    <input type="text" name="Description" class="form-control stylish-input" value="{{product.Description}}"><br>

                    <label for="" class="stylish-label">Image</label>
                    <img src="/product-images/{{product._id}}.jpg" alt="" width="100px" height="auto" id="imgView" class="stylish-img">
                    <input type="file" name="Image" class="form-control stylish-input" onchange="viewImage(event)" class="pt-3"><br>

                    <button type="submit" class="btn stylish-button">Submit</button>
                </form>
            </div>
        </div>
    </div>
</section>


<script>
    function viewImage(event){
        document.getElementById('imgView').src = URL.createObjectURL(event.target.files[0]);
    }
</script>

<style>
    /* Container Styling */
.container {
    background-color: #f8fff4;
    border-radius: 15px;
    padding: 40px;
    box-shadow: 0px 0px 20px rgba(0, 150, 0, 0.2);
    animation: fadeIn 1.2s ease-in-out;
}

/* Title Styling */
.animated-title {
    color: #008000;
    font-weight: bold;
    margin-bottom: 25px;
    text-transform: uppercase;
    animation: growTitle 1s ease-in-out;
    border-bottom: 2px solid #008000;
    padding-bottom: 5px;
}

/* Input Styling */
.stylish-input {
    border: 1px solid #008000;
    transition: 0.4s;
    border-radius: 10px;
    padding: 10px;
    background-color: white;
}

.stylish-input:focus {
    outline: none;
    background-color: #e0f7e0;
    border-color: #006400;
    box-shadow: 0px 0px 8px rgba(0, 150, 0, 0.6);
}

.stylish-label {
    color: #006400;
    font-weight: bold;
    display: inline-block;
    margin-top: 10px;
    transition: color 0.3s;
}

.stylish-label:hover {
    color: #004d00;
}

/* Button Styling */
.stylish-button {
    background-color: #006400;
    color: white;
    transition: 0.4s;
    padding: 10px 25px;
    border: none;
    border-radius: 50px;
    font-weight: bold;
    letter-spacing: 1px;
    margin-top: 15px;
    box-shadow: 0px 6px 15px rgba(0, 100, 0, 0.5);
}

.stylish-button:hover {
    background-color: white;
    color: #006400;
    border: 2px solid #006400;
    transform: scale(1.05);
    box-shadow: 0px 8px 20px rgba(0, 150, 0, 0.7);
}

/* Image Styling */
.stylish-img {
    border: 2px solid #008000;
    border-radius: 10px;
    padding: 5px;
    margin-top: 10px;
    transition: transform 0.4s, border 0.4s;
}

.stylish-img:hover {
    transform: rotate(5deg);
    border-color: #004d00;
}

/* Back Button Styling */
.back-button {
    display: inline-flex;
    align-items: center;
    font-size: 16px;
    padding: 8px 12px;
    border-radius: 25px;
    transition: background-color 0.3s, transform 0.2s;
}

.back-button i {
    margin-right: 5px;
    font-size: 18px;
}

.back-button:hover {
    background-color: green;
    color: white;
    transform: translateX(-2px);
}


/* Keyframe Animations */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: scale(0.9);
    }
    to {
        opacity: 1;
        transform: scale(1);
    }
}

@keyframes growTitle {
    from {
        letter-spacing: 2px;
        opacity: 0;
    }
    to {
        letter-spacing: 5px;
        opacity: 1;
    }
}

</style>